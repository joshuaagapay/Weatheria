const initState = {
  showWeatherDetails: false,
  showLoader: true,
  notification: null,
}

const rootReducer = (state = initState, action) => {
  console.log(action);
  switch(action.type){

  }
}

export default rootReducer